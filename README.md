# Daily Stack

A **don't break the chain** habit tracker built with Next.js.

See [`daily-stack/`](./daily-stack/) for the app source, setup instructions, and documentation.

```bash
cd daily-stack && npm install && npm run dev
```
